apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "citizen-service-api.fullname" . }}-config
data:
  APP_VERSION: {{ .Values.config.APP_VERSION | quote }}
  ENVIRONMENT: {{ .Values.config.ENVIRONMENT | quote }}
