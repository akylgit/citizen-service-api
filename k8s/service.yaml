apiVersion: v1
kind: Service
metadata:
  name: citizen-service-api
spec:
  selector:
    app: citizen-service-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
  type: NodePort
